type Props = {};
export const Stats = (props: Props) => {
    return (
        <footer className='stats' >
            <em>
                💼 You have X items on your list, and already packed X (X%)
            </em>

        </footer>
    );
};